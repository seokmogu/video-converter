# OpenAI API 설정
# OpenAI API 키를 여기에 입력하세요
OPENAI_API_KEY=your_openai_api_key_here

# 언어 설정
# 소스 언어 (비디오의 원본 언어) - Whisper 언어 코드 사용
# 예: en (영어), ja (일본어), ko (한국어), zh (중국어), fr (프랑스어), de (독일어), es (스페인어) 등
SOURCE_LANGUAGE=ja

# 타겟 언어 (번역할 언어) - 전체 언어명 사용
# 예: Korean, English, Japanese, Chinese, French, German, Spanish 등
TARGET_LANGUAGE=Korean

# OCR 언어 설정 (화면 텍스트 인식용)
# 여러 언어 지원 시 쉼표로 구분: en,ja 또는 en,ko,zh 등
OCR_LANGUAGES=ja,en

# 파일 경로 설정
# 기본 입력 비디오 파일 경로 (절대경로 또는 상대경로)
INPUT_VIDEO_PATH=./input_video.mp4

# 출력 디렉토리 (결과 파일들이 저장될 위치)
OUTPUT_DIR=./output

# 임시 파일 디렉토리 (처리 중 임시 파일들이 저장될 위치)
TEMP_DIR=./temp

# 기본 출력 비디오 확장자
DEFAULT_OUTPUT_FORMAT=mp4

# 지원하는 입력 비디오 확장자 (쉼표로 구분)
SUPPORTED_INPUT_FORMATS=mp4,mov,avi,mkv,wmv,flv,webm,m4v

# 파일 명명 패턴
# {stem}: 원본 파일명(확장자 제외), {timestamp}: 타임스탬프, {task}: 작업명
OUTPUT_FILENAME_PATTERN={stem}_{task}.{ext}

# 사용법:
# 1. 이 파일을 .env로 복사하세요: cp .env.example .env
# 2. .env 파일에서 각 설정값을 실제 값으로 변경하세요
# 3. OpenAI API 키는 https://platform.openai.com/api-keys 에서 얻을 수 있습니다
# 4. 언어 코드는 README.md의 "지원 언어" 섹션을 참고하세요
# 5. 파일 경로는 절대경로 또는 프로젝트 기준 상대경로로 설정하세요